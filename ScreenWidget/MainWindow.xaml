<Window x:Class="ScreenWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp">

    <Grid x:Name="RootGrid">

        <!-- ① Spinning rainbow gradient – the animated border colour source -->
        <Rectangle x:Name="AnimBorderRect"
                   RadiusX="14"
                RadiusY="14"
                   RenderTransformOrigin="0.5,0.5">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0"
                        EndPoint="1,1">
                    <GradientStop Color="#FF0080"
                            Offset="0"/>
                    <GradientStop Color="#8000FF"
                            Offset="0.2"/>
                    <GradientStop Color="#0080FF"
                            Offset="0.4"/>
                    <GradientStop Color="#00FFB2"
                            Offset="0.6"/>
                    <GradientStop Color="#FFDD00"
                            Offset="0.8"/>
                    <GradientStop Color="#FF0080"
                            Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
            <Rectangle.RenderTransform>
                <RotateTransform x:Name="BorderRotate"/>
            </Rectangle.RenderTransform>
        </Rectangle>

        <!-- ② Inner content area (bg colour + margin = visible border width) -->
        <Border x:Name="ContentBorder"
                Margin="4"
                CornerRadius="11"
                Background="#1A1A2E"
                ClipToBounds="True">
            <Grid>
                <!-- Camera image -->
                <Image x:Name="CamImage"
                        Stretch="UniformToFill"/>

                <!-- Placeholder when no image captured yet -->
                <StackPanel x:Name="NoImagePanel"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Margin="20">
                    <TextBlock Text="📷"
                               FontSize="44"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="No capture yet"
                               FontFamily="Segoe UI"
                            FontSize="12"
                               Foreground="#88AAAACC"
                               HorizontalAlignment="Center"
                               Margin="0 6 0 0"/>
                </StackPanel>

                <!-- Bottom info bar -->
                <Border Background="#AA0D0D1A"
                        VerticalAlignment="Bottom"
                        Padding="8 4">
                    <Grid>
                        <TextBlock x:Name="CaptureTimeLabel"
                                   FontFamily="Segoe UI"
                                FontSize="10"
                                   Foreground="#88AAAACC"/>
                        <TextBlock x:Name="NextCaptureLabel"
                                   FontFamily="Segoe UI"
                                FontSize="10"
                                   Foreground="#88AAAACC"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>

    </Grid>
</Window>
